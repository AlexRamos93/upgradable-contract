{
  "name": "upgradable-contract",
  "scripts": {
    "test": "npx hardhat test",
    "verify": "npx hardhat verify --network goerli",
    "deploy": "npx hardhat run scripts/deploy.ts --network goerli",
    "upgrade": "npx hardhat run scripts/upgrade.ts --network goerli"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/hardhat-upgrades": "^1.21.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.11.2"
  }
}
